version: 0.2

phases:
  build:
    commands:
      - echo "Deploying static site"

  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id E3FI9W33EEJ5MS --paths "/*"

artifacts:
  files:
    - '**/*'
